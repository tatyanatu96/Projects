def collatz(number):
    list=[]
    list.append(number)
    while number!=1:
        if number%2==0:
            number=number // 2
            list.append(number)
        else:
            number=3*number+1
            list.append(number)
    print(*list, sep='\n')

collatz(10)
